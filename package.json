{
  "name": "mcp-abap-adt",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "( if not exist dist mkdir dist || true ) && tsc && node -e \"require('fs').chmodSync('dist/index.js', '755')\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.3.2",
    "@types/node": "^22.10.7",
    "axios": "^1.7.9",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "ini": "^5.0.0",
    "tsc": "^2.0.4"
  },
  "description": ""
}
